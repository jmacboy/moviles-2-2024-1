import { PersonaUpgradeStatements } from './persona.upgrade.statements';

describe('PersonaUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new PersonaUpgradeStatements()).toBeTruthy();
  });
});
